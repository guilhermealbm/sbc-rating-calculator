<?xml version="1.0" encoding="utf-8"?>

<layout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools">

    <data>
        <variable
            name="clickListener"
            type="android.view.View.OnClickListener"/>
        <variable
            name="playerRating"
            type="com.guilhermealbm.sbcratingcalculator.model.PlayerRating"/>
    </data>

    <androidx.constraintlayout.widget.ConstraintLayout
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:padding="4dp">

        <Button
            android:id="@+id/sub_btn"
            android:text="@string/sub_btn"
            android:layout_width="36dp"
            android:layout_height="36dp"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintTop_toTopOf="parent"
            />

        <TextView
            android:id="@+id/player_rating_text"
            tools:text="0 x 84"
            android:text="@{playerRating.players + ` x ` + playerRating.rating}"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            app:layout_constraintStart_toEndOf="@id/sub_btn"
            app:layout_constraintBottom_toBottomOf="@id/sub_btn"
            app:layout_constraintTop_toTopOf="parent"
            android:padding="4dp"
            android:textSize="16sp"
            />

        <Button
            android:id="@+id/add_btn"
            android:text="@string/add_btn"
            android:onClick="@{clickListener}"
            android:layout_width="36dp"
            android:layout_height="36dp"
            app:layout_constraintStart_toEndOf="@id/player_rating_text"
            app:layout_constraintTop_toTopOf="parent"
            />

    </androidx.constraintlayout.widget.ConstraintLayout>

</layout>